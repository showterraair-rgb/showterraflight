# Show Terra Air — Public website production build
# Copy to frontend-public/.env.production before npm run build
#
# Default /api/v1 works when Nginx proxies /api on showterraair.com (recommended).

VITE_API_BASE_URL=/api/v1
