# Show Terra Air — Admin panel production build
# Copy to frontend-admin/.env.production before npm run build
#
# Default /api/v1 works when Nginx proxies /api on admin.showterraair.com (recommended).
# Only set VITE_API_BASE_URL if API is on a different origin without proxy.

VITE_API_BASE_URL=/api/v1
